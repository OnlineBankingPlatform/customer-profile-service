-- Initial schema for customer profile service
-- This migration creates the baseline database structure

-- Add your initial tables here
-- Example:
-- CREATE TABLE customer (
--     id BIGSERIAL PRIMARY KEY,
--     name VARCHAR(255) NOT NULL,
--     email VARCHAR(255) UNIQUE NOT NULL,
--     created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
--     updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
-- );

-- Placeholder to allow Flyway to run successfully
-- Remove this comment and add your actual schema when ready
